{
	"dockercommand": "sudo docker",
	"commands": {
		"base": {
			"info": "info",
			"list|List currently installed containers": "ps -a",
			"stats": "stats"
		},
		"containers": {
			"busybox": {
				"run": "run -it --rm busybox"
			},
			"sql1": {
				"start": "restart sql1",
				"stop": "stop sql1",
				"bash": "exec -it sql1 'bash'",
				"logs": "logs sql1",
				"processes": "top sql1"
			}
		}
	}
}